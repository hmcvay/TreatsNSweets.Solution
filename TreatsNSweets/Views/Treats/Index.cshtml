@{
  Layout = "_Layout";
}

@using TreatsNSweets.Models;
@using System;
@model List<TreatsNSweets.Models.Treat>;

<h1>Treats</h1>

@if (@Model.Count == 0)
{
  <h3>none</h3>
}
else
{
  <ul>
    @foreach (Treat treat in Model)
    {
      <li>@Html.ActionLink($"{treat.Name}", "Details", new { id = treat.TreatId})</li>
    }
  </ul>
}
else
{
  <h3>No treats have been added yet!</h3>
}
